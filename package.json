{
  "name": "nexgen-discord-setup",
  "version": "2.0.0",
  "private": true,
  "description": "Bilingual (EN/ID) Discord server auto-setup for NexGen Development",
  "main": "setup-server.js",
  "scripts": {
    "start": "node setup-server.js",
    "setup:only": "node setup-server.js --setup-only",
    "refresh": "node setup-server.js --refresh"
  },
  "dependencies": {
    "@vercel/functions": "^3.9.9",
    "discord-interactions": "^4.4.0",
    "discord.js": "^14.27.0",
    "dotenv": "^16.6.1"
  },
  "engines": {
    "node": "24.x"
  }
}
